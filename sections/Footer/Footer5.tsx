import Footer, { Props } from "../../components/footer/Footer.tsx";

export default function Section(props: Props) {
  return (
    <Footer
      variation="Variation 5"
      {...props}
    />
  );
}
