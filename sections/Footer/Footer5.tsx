import Footer, { Props } from "$store/components/footer/Footer1.tsx";

export default function Section(props: Props) {
  return (
    <Footer 
      variation="Variation 5"
      {...props}
    />
  );
}
